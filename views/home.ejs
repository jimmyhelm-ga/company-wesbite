<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= COMPANY.name %></title>
</head>
<%- include('./partials/nav') %>
<body>
    <h1>Welcome To <%= COMPANY.name %>!</h1>
    <p><%= COMPANY.address %></p>
    <p><%= COMPANY.phone %></p>
    <% if(COMPANY.isOpen === true) { %>
        <p>Open to Game!</p>
      <% } else { %>
        <p>Sorry, we are closed.</p>
      <% } %>
 
</body>
</html>